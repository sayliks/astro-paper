---
import IconChevronLeft from "@/assets/icons/IconChevronLeft.svg";
import LinkButton from "@/components/LinkButton.astro";
import { getRelativeLocaleUrl } from "astro:i18n";
import { useTranslations } from "@/i18n";
import config from "@/config";

const locale = Astro.currentLocale ?? config.site.lang;
const t = useTranslations(locale);
---

{
  config.features.showBackButton && (
    <div class="app-layout flex items-center justify-start">
      <LinkButton
        id="back-button"
        href={getRelativeLocaleUrl(locale, "")}
        class="text-muted-foreground hover:text-foreground -ms-1 mt-8 mb-4 items-center gap-1.5"
      >
        <IconChevronLeft class="inline-block size-5 rtl:rotate-180" />
        <span>{t.post.goBack}</span>
      </LinkButton>
    </div>
  )
}
