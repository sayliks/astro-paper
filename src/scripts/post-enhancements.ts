/**
 * Consolidated post-page enhancements.
 * Combines heading-links, code-copy, and image-fade into a single module
 * to reduce HTTP requests and module evaluation overhead on article pages.
 */

import "./heading-links";
import "./code-copy";
import "./image-fade";
